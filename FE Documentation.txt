Public{
    Has all images used

    index.html: responsible for the whole react program. 
    Constructs the foundation of the website using html, bootsrap.
    Linkes with manifest.json

    manifest.json: responsible for colors, app behaviour
    does the progrsseive web application development (pwa): prototyping, redirecting, rendering

    robots.txt: resonsible for search engine(google button)
}


src{
    components{
        Button, footer, header (index.jsx) main code for them
        index.jsx loads the code for each components
    }
    pages{
        hom

    }
    }


tailwind:framework used to convert from figma to css interface
